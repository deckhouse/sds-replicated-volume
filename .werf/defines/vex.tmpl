{{- define "vex mitigation" }}
  {{- $context := index . 0 }}
  {{- $imageName := index . 1 }}
---
image: vex-artifact
fromImage: base/vex
final: true

git:
- add: /images/{{ $imageName }}/known_vulnerabilities.vex
  to: /known_vulnerabilities.vex{{ printf "image: %s" $imageName | nindent 2 }}
  stageDependencies:
    install:
    - "**/*"
{{- end }}

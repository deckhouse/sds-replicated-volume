{{- define "vex mitigation" }}
  {{- $context := index . 0 }}
  {{- $imageName := index . 1 }}
  {{- $cont := index . 2 }}
---
image: vex-artifact
fromImage: base/vex
final: true

git:
- add: /images/{{ $imageName }}/known_vulnerabilities.vex
  to: /known_vulnerabilities.vex{{ printf "image: %s" $cont | nindent 2 }}
  stageDependencies:
    install:
    - "**/*"
{{- end }}

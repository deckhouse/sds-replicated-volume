{{- define "sds_replicated_volume_agent_resources" }}
cpu: 50m
memory: 50Mi
{{- end }}

{{- define "drbd_prometheus_exporter_agent_resources" }}
cpu: 50m
memory: 25Mi
{{- end }}

{{- if .Values.sdsReplicatedVolume.internal.newControlPlane }}
{{- if (.Values.global.enabledModules | has "vertical-pod-autoscaler-crd") }}
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: agent
  namespace: d8-{{ .Chart.Name }}
  {{- include "helm_lib_module_labels" (list . (dict "app" "agent")) | nindent 2 }}
spec:
  targetRef:
    apiVersion: "apps/v1"
    kind: DaemonSet
    name: agent
  updatePolicy:
    updateMode: "Auto"
  resourcePolicy:
    containerPolicies:
    - containerName: "agent"
      minAllowed:
        {{- include "sds_replicated_volume_agent_resources" . | nindent 8 }}
      maxAllowed:
        cpu: 200m
        memory: 100Mi
    {{- include "helm_lib_vpa_kube_rbac_proxy_resources" . | nindent 4 }}
    - containerName: drbd-prometheus-exporter
      minAllowed:
        {{- include "drbd_prometheus_exporter_agent_resources" . | nindent 8 }}
      maxAllowed:
        cpu: 100m
        memory: 50Mi
    - containerName: kube-rbac-proxy-drbd
      minAllowed:
        {{- include "helm_lib_container_kube_rbac_proxy_resources" . | nindent 8 }}
      maxAllowed:
        cpu: 20m
        memory: 25Mi
{{- end }}
{{- end }}

{{- if .Values.sdsReplicatedVolume.internal.newControlPlane }}
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: agent
  namespace: d8-{{ .Chart.Name }}
  {{- include "helm_lib_module_labels" (list . (dict "app" "agent")) | nindent 2 }}
spec:
  selector:
    matchLabels:
      app: agent
  template:
    metadata:
      name: agent
      namespace: d8-{{ .Chart.Name }}
      {{- include "helm_lib_module_labels" (list . (dict "app" "agent")) | nindent 6 }}
    spec:
      {{- include "helm_lib_priority_class" (tuple . "cluster-medium") | nindent 6 }}
      {{- include "helm_lib_tolerations" (tuple . "any-node" "storage-problems") | nindent 6 }}
      {{- include "helm_lib_module_pod_security_context_run_as_user_root" . | nindent 6 }}
      affinity: {}
      nodeSelector:
        storage.deckhouse.io/sds-replicated-volume-node: ""
      dnsPolicy: ClusterFirstWithHostNet
      imagePullSecrets:
        - name: {{ .Chart.Name }}-module-registry
      serviceAccountName: agent
      hostNetwork: true
      containers:
      - name: agent
        image: {{ include "helm_lib_module_image" (list . "agent") }}
        imagePullPolicy: IfNotPresent
        readinessProbe:
          httpGet:
            path: /readyz
            host: 127.0.0.1
            port: 4269
            scheme: HTTP
          initialDelaySeconds: 5
          failureThreshold: 2
          periodSeconds: 1
        livenessProbe:
          httpGet:
            path: /healthz
            host: 127.0.0.1
            port: 4269
            scheme: HTTP
          periodSeconds: 1
          failureThreshold: 3
        env:
          - name: NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: DRBD_MIN_PORT
            value: "{{ $.Values.sdsReplicatedVolume.drbdPortRange.minPort }}"
          - name: DRBD_MAX_PORT
            value: "{{ $.Values.sdsReplicatedVolume.drbdPortRange.maxPort }}"
          - name: SLOGH_CONFIG_PATH
            value: "/etc/config/slogh.cfg"
        securityContext:
          privileged: true
          readOnlyRootFilesystem: true
        volumeMounts:
        - mountPath: /dev/
          name: host-device-dir
        - mountPath: /dev/sdsrv/
          name: dev-sdsrv
        - mountPath: /sys/
          name: sys-dir
        - mountPath: /etc/config/
          name: config
        - mountPath: /var/run/drbd/lock/
          name: var-run-drbd-lock
        ports:
          - containerPort: 4215
            hostPort: 4215
            name: prometheus
            protocol: TCP
        resources:
          requests:
            {{- include "helm_lib_module_ephemeral_storage_only_logs" . | nindent 12 }}
            {{- if not ( .Values.global.enabledModules | has "vertical-pod-autoscaler-crd") }}
              {{- include "sds_replicated_volume_agent_resources" . | nindent 12 }}
            {{- end }}
      - name: kube-rbac-proxy
        image: {{ include "helm_lib_module_common_image" (list . "kubeRbacProxy") }}
        imagePullPolicy: IfNotPresent
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        args:
          - --secure-listen-address=$(KUBE_RBAC_PROXY_LISTEN_ADDRESS):4215
          - --v=2
          - --logtostderr=true
          - --stale-cache-interval=1h30m
          - "--livez-path=/livez"
        env:
          - name: KUBE_RBAC_PROXY_LISTEN_ADDRESS
            valueFrom:
              fieldRef:
                apiVersion: v1
                fieldPath: status.podIP
          - name: KUBE_RBAC_PROXY_CONFIG
            value: |
              upstreams:
              - upstream: http://127.0.0.1:4270
                path: /
                authorization:
                  resourceAttributes:
                    namespace: d8-{{ .Chart.Name }}
                    apiGroup: apps
                    apiVersion: v1
                    resource: daemonsets
                    subresource: prometheus-metrics
                    name: agent
        livenessProbe:
          httpGet:
            path: /livez
            port: 4215
            scheme: HTTPS
        readinessProbe:
          httpGet:
            path: /livez
            port: 4215
            scheme: HTTPS
        resources:
          requests:
            {{- include "helm_lib_module_ephemeral_storage_only_logs" . | nindent 12 }}
            {{- if not ( .Values.global.enabledModules | has "vertical-pod-autoscaler-crd") }}
              {{- include "helm_lib_container_kube_rbac_proxy_resources" . | nindent 12 }}
            {{- end }}
        {{- include "helm_lib_module_container_security_context_read_only_root_filesystem_capabilities_drop_all" . | nindent 8 }}
      - name: drbd-prometheus-exporter
        image: {{ include "helm_lib_module_image" (list . "drbdReactor") }}
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            host: 127.0.0.1
            path: /
            port: 4274
            scheme: HTTP
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        resources:
          requests:
            {{- include "helm_lib_module_ephemeral_storage_only_logs" . | nindent 12 }}
            {{- if not ( .Values.global.enabledModules | has "vertical-pod-autoscaler-crd") }}
              {{- include "drbd_prometheus_exporter_agent_resources" . | nindent 12 }}
            {{- end }}
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          seccompProfile:
            type: RuntimeDefault
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
          - mountPath: /etc/drbd-reactor.d/
            name: drbd-reactor-config
      - name: kube-rbac-proxy-drbd
        image: {{ include "helm_lib_module_common_image" (list . "kubeRbacProxy") }}
        imagePullPolicy: IfNotPresent
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        args:
          - --secure-listen-address=$(KUBE_RBAC_PROXY_LISTEN_ADDRESS):4273
          - --v=2
          - --logtostderr=true
          - --stale-cache-interval=1h30m
          - "--livez-path=/livez"
        env:
          - name: KUBE_RBAC_PROXY_LISTEN_ADDRESS
            valueFrom:
              fieldRef:
                apiVersion: v1
                fieldPath: status.podIP
          - name: KUBE_RBAC_PROXY_CONFIG
            value: |
              upstreams:
              - upstream: http://127.0.0.1:4274
                path: /
                authorization:
                  resourceAttributes:
                    namespace: d8-{{ .Chart.Name }}
                    apiGroup: apps
                    apiVersion: v1
                    resource: daemonsets
                    subresource: prometheus-metrics
                    name: agent
        ports:
          - containerPort: 4273
            hostPort: 4273
            name: drbd-metrics
            protocol: TCP
        livenessProbe:
          httpGet:
            path: /livez
            port: 4273
            scheme: HTTPS
        readinessProbe:
          httpGet:
            path: /livez
            port: 4273
            scheme: HTTPS
        resources:
          requests:
            {{- include "helm_lib_module_ephemeral_storage_only_logs" . | nindent 12 }}
            {{- if not ( .Values.global.enabledModules | has "vertical-pod-autoscaler-crd") }}
              {{- include "helm_lib_container_kube_rbac_proxy_resources" . | nindent 12 }}
            {{- end }}
        {{- include "helm_lib_module_container_security_context_read_only_root_filesystem_capabilities_drop_all" . | nindent 8 }}
      volumes:
      - hostPath:
          path: /dev/
          type: ""
        name: host-device-dir
      - hostPath:
          path: /sys/
          type: Directory
        name: sys-dir
      - name: config
        configMap:
          name: agent-config
          items:
            - key: slogh.cfg
              path: slogh.cfg
      - name: dev-sdsrv
        hostPath:
          path: /dev/sdsrv/
          type: DirectoryOrCreate
      - name: var-run-drbd-lock
        emptyDir: {}
      - name: drbd-reactor-config
        configMap:
          defaultMode: 420
          name: agent-monitoring
{{- end }}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    module: sds-replicated-volume
  name: replicatedvolumesnapshots.storage.deckhouse.io
spec:
  group: storage.deckhouse.io
  names:
    kind: ReplicatedVolumeSnapshot
    listKind: ReplicatedVolumeSnapshotList
    plural: replicatedvolumesnapshots
    shortNames:
    - rvs
    singular: replicatedvolumesnapshot
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicatedVolumeName
      name: Volume
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyToUse
      name: ReadyToUse
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              replicatedVolumeName:
                maxLength: 120
                minLength: 1
                pattern: ^[0-9A-Za-z.+_-]*$
                type: string
                x-kubernetes-validations:
                - message: replicatedVolumeName is immutable
                  rule: self == oldSelf
            required:
            - replicatedVolumeName
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              creationTime:
                format: date-time
                type: string
              datamesh:
                properties:
                  members:
                    default: []
                    items:
                      properties:
                        name:
                          maxLength: 253
                          minLength: 1
                          type: string
                        nodeName:
                          maxLength: 253
                          minLength: 1
                          type: string
                        ready:
                          default: false
                          type: boolean
                        snapshotHandle:
                          maxLength: 253
                          type: string
                      required:
                      - name
                      - nodeName
                      - ready
                      type: object
                    maxItems: 32
                    type: array
                    x-kubernetes-list-type: atomic
                    x-kubernetes-validations:
                    - message: members[].name must be unique
                      rule: self.all(x, self.exists_one(y, x.name == y.name))
                  readyCount:
                    default: 0
                    minimum: 0
                    type: integer
                  replicatedVolumeName:
                    maxLength: 120
                    minLength: 1
                    type: string
                  totalCount:
                    default: 0
                    minimum: 0
                    type: integer
                required:
                - members
                - readyCount
                - replicatedVolumeName
                - totalCount
                type: object
              message:
                maxLength: 512
                type: string
              phase:
                enum:
                - Pending
                - InProgress
                - Synchronizing
                - Ready
                - Failed
                - Deleting
                type: string
              prepareRevision:
                format: int64
                type: integer
              prepareTransitions:
                items:
                  description: |-
                    ReplicatedVolumeDatameshTransition represents an active datamesh transition.
                    Each transition has an ordered list of steps that are pre-declared on creation
                    and executed sequentially. The transition's start time is steps[0].startedAt.
                  properties:
                    fromReplicaType:
                      description: |-
                        FromReplicaType is the source replica type for ChangeReplicaType transitions.
                        Required for ChangeReplicaType. Must not be set for other types.
                      enum:
                      - Diskful
                      - Access
                      - TieBreaker
                      - ShadowDiskful
                      type: string
                    fromSystemNetworkNames:
                      description: |-
                        FromSystemNetworkNames is the source set of system network names for
                        ChangeSystemNetworks transitions. Captured from datamesh.systemNetworkNames
                        at dispatch time. Required for ChangeSystemNetworks. Must not be set for
                        other types.
                      items:
                        maxLength: 64
                        type: string
                      maxItems: 10
                      type: array
                      x-kubernetes-list-type: atomic
                    group:
                      description: |-
                        Group is the concurrency group for this transition.
                        Set by the controller at transition creation time. Read-only for users.
                        Provides observability: explains why a transition may be waiting.
                      enum:
                      - Attachment
                      - Emergency
                      - Formation
                      - Multiattach
                      - Network
                      - NonVotingMembership
                      - Quorum
                      - Resize
                      - Sync
                      - VotingMembership
                      type: string
                    planID:
                      description: |-
                        PlanID identifies the transition plan used to create this transition.
                        Used by the engine to look up step callbacks from the plan registry.
                      minLength: 1
                      type: string
                    replicaName:
                      description: |-
                        ReplicaName is the name of the replica this transition applies to.
                        Required for AddReplica, RemoveReplica, ChangeReplicaType, Attach, Detach, ForceRemoveReplica, ForceDetach.
                        Must not be set for Formation, EnableMultiattach, DisableMultiattach, ChangeQuorum.
                      maxLength: 123
                      minLength: 1
                      pattern: ^.+-([0-9]|[12][0-9]|3[01])$
                      type: string
                    replicaType:
                      description: |-
                        ReplicaType is the target replica type for AddReplica/RemoveReplica/ForceRemoveReplica.
                        Uses ReplicaType (Diskful, Access, TieBreaker, ShadowDiskful) — liminal states
                        (LiminalDiskful, LiminalShadowDiskful) are internal mechanics visible in step names,
                        not in transition parameters.
                        Required for AddReplica, RemoveReplica, ForceRemoveReplica. Must not be set for other types.
                      enum:
                      - Diskful
                      - Access
                      - TieBreaker
                      - ShadowDiskful
                      type: string
                    steps:
                      description: |-
                        Steps is the ordered list of steps for this transition.
                        All steps are written when the transition is created (with status=Pending,
                        and the first step immediately set to Active with startedAt).
                        Steps are executed sequentially — only one step is Active at a time.
                        Must have at least one element.
                      items:
                        description: ReplicatedVolumeDatameshTransitionStep represents
                          one step within a transition.
                        properties:
                          completedAt:
                            description: CompletedAt is set when the step transitions
                              from Active to Completed.
                            format: date-time
                            type: string
                          datameshRevision:
                            description: |-
                              DatameshRevision is the revision assigned when this step was activated.
                              0 if the step has not been activated yet or does not bump the revision.
                            format: int64
                            type: integer
                          message:
                            description: |-
                              Message is a per-step progress or error message.
                              Updated every reconciliation while the step is Active.
                            maxLength: 512
                            type: string
                          name:
                            description: Name is a human-readable step name (e.g.,
                              "Preconfigure", "attach", "✦ → D∅").
                            maxLength: 64
                            minLength: 1
                            type: string
                          startedAt:
                            description: StartedAt is set when the step transitions
                              from Pending to Active.
                            format: date-time
                            type: string
                          status:
                            default: Pending
                            description: Status is the current status of this step.
                            enum:
                            - Pending
                            - Active
                            - Completed
                            type: string
                        required:
                        - name
                        - status
                        type: object
                      maxItems: 8
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    toReplicaType:
                      description: |-
                        ToReplicaType is the target replica type for ChangeReplicaType transitions.
                        Required for ChangeReplicaType. Must not be set for other types.
                      enum:
                      - Diskful
                      - Access
                      - TieBreaker
                      - ShadowDiskful
                      type: string
                    toSystemNetworkNames:
                      description: |-
                        ToSystemNetworkNames is the target set of system network names for
                        ChangeSystemNetworks transitions. Captured from RSP configuration at
                        dispatch time. Required for ChangeSystemNetworks. Must not be set for
                        other types.
                      items:
                        maxLength: 64
                        type: string
                      maxItems: 10
                      type: array
                      x-kubernetes-list-type: atomic
                    type:
                      description: Type is the transition type.
                      enum:
                      - AddReplica
                      - Attach
                      - ChangeQuorum
                      - ChangeReplicaType
                      - ChangeSystemNetworks
                      - Detach
                      - DisableMultiattach
                      - EnableMultiattach
                      - ForceDetach
                      - ForceRemoveReplica
                      - Formation
                      - PrepareSnapshot
                      - RemoveReplica
                      - RepairNetworkAddresses
                      - ResizeVolume
                      - SyncSnapshot
                      type: string
                  required:
                  - group
                  - steps
                  - type
                  type: object
                  x-kubernetes-validations:
                  - message: replicaName is required for AddReplica, RemoveReplica,
                      ChangeReplicaType, Attach, Detach, ForceRemoveReplica, ForceDetach
                      transitions
                    rule: self.type == 'Formation' || self.type == 'EnableMultiattach'
                      || self.type == 'DisableMultiattach' || self.type == 'ChangeQuorum'
                      || self.type == 'RepairNetworkAddresses' || self.type == 'ChangeSystemNetworks'
                      || self.type == 'ResizeVolume' || has(self.replicaName)
                  - message: replicaName must not be set for Formation, EnableMultiattach,
                      DisableMultiattach, ChangeQuorum, RepairNetworkAddresses, ChangeSystemNetworks,
                      ResizeVolume transitions
                    rule: '!has(self.replicaName) || !(self.type == ''Formation''
                      || self.type == ''EnableMultiattach'' || self.type == ''DisableMultiattach''
                      || self.type == ''ChangeQuorum'' || self.type == ''RepairNetworkAddresses''
                      || self.type == ''ChangeSystemNetworks'' || self.type == ''ResizeVolume'')'
                  - message: replicaType must only be set for AddReplica, RemoveReplica,
                      ForceRemoveReplica transitions
                    rule: self.type == 'AddReplica' || self.type == 'RemoveReplica'
                      || self.type == 'ForceRemoveReplica' || !has(self.replicaType)
                  - message: replicaType is required for AddReplica, RemoveReplica,
                      ForceRemoveReplica transitions
                    rule: '!(self.type == ''AddReplica'' || self.type == ''RemoveReplica''
                      || self.type == ''ForceRemoveReplica'') || has(self.replicaType)'
                  - message: fromReplicaType and toReplicaType must only be set for
                      ChangeReplicaType transitions
                    rule: self.type == 'ChangeReplicaType' || (!has(self.fromReplicaType)
                      && !has(self.toReplicaType))
                  - message: fromReplicaType and toReplicaType are required for ChangeReplicaType
                      transitions
                    rule: self.type != 'ChangeReplicaType' || (has(self.fromReplicaType)
                      && has(self.toReplicaType))
                  - message: fromSystemNetworkNames must only be set for ChangeSystemNetworks
                      transitions
                    rule: self.type == 'ChangeSystemNetworks' || !has(self.fromSystemNetworkNames)
                  - message: toSystemNetworkNames must only be set for ChangeSystemNetworks
                      transitions
                    rule: self.type == 'ChangeSystemNetworks' || !has(self.toSystemNetworkNames)
                  - message: fromSystemNetworkNames is required for ChangeSystemNetworks
                      transitions
                    rule: self.type != 'ChangeSystemNetworks' || has(self.fromSystemNetworkNames)
                  - message: toSystemNetworkNames is required for ChangeSystemNetworks
                      transitions
                    rule: self.type != 'ChangeSystemNetworks' || has(self.toSystemNetworkNames)
                type: array
                x-kubernetes-list-type: atomic
              readyToUse:
                type: boolean
              sourceReplicaSnapshotName:
                description: |-
                  SourceReplicaSnapshotName is the RVRS created from the attached (Primary)
                  replica. This snapshot has the most up-to-date data and is preferred
                  as the sync source and clone source.
                maxLength: 253
                type: string
              syncDRBDResources:
                description: |-
                  SyncDRBDResources lists temporary DRBDResource names created for
                  snapshot synchronization. The RVS controller uses this to monitor
                  sync progress and clean up resources after sync completes.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              syncRevision:
                format: int64
                type: integer
              syncTransitions:
                items:
                  description: |-
                    ReplicatedVolumeDatameshTransition represents an active datamesh transition.
                    Each transition has an ordered list of steps that are pre-declared on creation
                    and executed sequentially. The transition's start time is steps[0].startedAt.
                  properties:
                    fromReplicaType:
                      description: |-
                        FromReplicaType is the source replica type for ChangeReplicaType transitions.
                        Required for ChangeReplicaType. Must not be set for other types.
                      enum:
                      - Diskful
                      - Access
                      - TieBreaker
                      - ShadowDiskful
                      type: string
                    fromSystemNetworkNames:
                      description: |-
                        FromSystemNetworkNames is the source set of system network names for
                        ChangeSystemNetworks transitions. Captured from datamesh.systemNetworkNames
                        at dispatch time. Required for ChangeSystemNetworks. Must not be set for
                        other types.
                      items:
                        maxLength: 64
                        type: string
                      maxItems: 10
                      type: array
                      x-kubernetes-list-type: atomic
                    group:
                      description: |-
                        Group is the concurrency group for this transition.
                        Set by the controller at transition creation time. Read-only for users.
                        Provides observability: explains why a transition may be waiting.
                      enum:
                      - Attachment
                      - Emergency
                      - Formation
                      - Multiattach
                      - Network
                      - NonVotingMembership
                      - Quorum
                      - Resize
                      - Sync
                      - VotingMembership
                      type: string
                    planID:
                      description: |-
                        PlanID identifies the transition plan used to create this transition.
                        Used by the engine to look up step callbacks from the plan registry.
                      minLength: 1
                      type: string
                    replicaName:
                      description: |-
                        ReplicaName is the name of the replica this transition applies to.
                        Required for AddReplica, RemoveReplica, ChangeReplicaType, Attach, Detach, ForceRemoveReplica, ForceDetach.
                        Must not be set for Formation, EnableMultiattach, DisableMultiattach, ChangeQuorum.
                      maxLength: 123
                      minLength: 1
                      pattern: ^.+-([0-9]|[12][0-9]|3[01])$
                      type: string
                    replicaType:
                      description: |-
                        ReplicaType is the target replica type for AddReplica/RemoveReplica/ForceRemoveReplica.
                        Uses ReplicaType (Diskful, Access, TieBreaker, ShadowDiskful) — liminal states
                        (LiminalDiskful, LiminalShadowDiskful) are internal mechanics visible in step names,
                        not in transition parameters.
                        Required for AddReplica, RemoveReplica, ForceRemoveReplica. Must not be set for other types.
                      enum:
                      - Diskful
                      - Access
                      - TieBreaker
                      - ShadowDiskful
                      type: string
                    steps:
                      description: |-
                        Steps is the ordered list of steps for this transition.
                        All steps are written when the transition is created (with status=Pending,
                        and the first step immediately set to Active with startedAt).
                        Steps are executed sequentially — only one step is Active at a time.
                        Must have at least one element.
                      items:
                        description: ReplicatedVolumeDatameshTransitionStep represents
                          one step within a transition.
                        properties:
                          completedAt:
                            description: CompletedAt is set when the step transitions
                              from Active to Completed.
                            format: date-time
                            type: string
                          datameshRevision:
                            description: |-
                              DatameshRevision is the revision assigned when this step was activated.
                              0 if the step has not been activated yet or does not bump the revision.
                            format: int64
                            type: integer
                          message:
                            description: |-
                              Message is a per-step progress or error message.
                              Updated every reconciliation while the step is Active.
                            maxLength: 512
                            type: string
                          name:
                            description: Name is a human-readable step name (e.g.,
                              "Preconfigure", "attach", "✦ → D∅").
                            maxLength: 64
                            minLength: 1
                            type: string
                          startedAt:
                            description: StartedAt is set when the step transitions
                              from Pending to Active.
                            format: date-time
                            type: string
                          status:
                            default: Pending
                            description: Status is the current status of this step.
                            enum:
                            - Pending
                            - Active
                            - Completed
                            type: string
                        required:
                        - name
                        - status
                        type: object
                      maxItems: 8
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    toReplicaType:
                      description: |-
                        ToReplicaType is the target replica type for ChangeReplicaType transitions.
                        Required for ChangeReplicaType. Must not be set for other types.
                      enum:
                      - Diskful
                      - Access
                      - TieBreaker
                      - ShadowDiskful
                      type: string
                    toSystemNetworkNames:
                      description: |-
                        ToSystemNetworkNames is the target set of system network names for
                        ChangeSystemNetworks transitions. Captured from RSP configuration at
                        dispatch time. Required for ChangeSystemNetworks. Must not be set for
                        other types.
                      items:
                        maxLength: 64
                        type: string
                      maxItems: 10
                      type: array
                      x-kubernetes-list-type: atomic
                    type:
                      description: Type is the transition type.
                      enum:
                      - AddReplica
                      - Attach
                      - ChangeQuorum
                      - ChangeReplicaType
                      - ChangeSystemNetworks
                      - Detach
                      - DisableMultiattach
                      - EnableMultiattach
                      - ForceDetach
                      - ForceRemoveReplica
                      - Formation
                      - PrepareSnapshot
                      - RemoveReplica
                      - RepairNetworkAddresses
                      - ResizeVolume
                      - SyncSnapshot
                      type: string
                  required:
                  - group
                  - steps
                  - type
                  type: object
                  x-kubernetes-validations:
                  - message: replicaName is required for AddReplica, RemoveReplica,
                      ChangeReplicaType, Attach, Detach, ForceRemoveReplica, ForceDetach
                      transitions
                    rule: self.type == 'Formation' || self.type == 'EnableMultiattach'
                      || self.type == 'DisableMultiattach' || self.type == 'ChangeQuorum'
                      || self.type == 'RepairNetworkAddresses' || self.type == 'ChangeSystemNetworks'
                      || self.type == 'ResizeVolume' || has(self.replicaName)
                  - message: replicaName must not be set for Formation, EnableMultiattach,
                      DisableMultiattach, ChangeQuorum, RepairNetworkAddresses, ChangeSystemNetworks,
                      ResizeVolume transitions
                    rule: '!has(self.replicaName) || !(self.type == ''Formation''
                      || self.type == ''EnableMultiattach'' || self.type == ''DisableMultiattach''
                      || self.type == ''ChangeQuorum'' || self.type == ''RepairNetworkAddresses''
                      || self.type == ''ChangeSystemNetworks'' || self.type == ''ResizeVolume'')'
                  - message: replicaType must only be set for AddReplica, RemoveReplica,
                      ForceRemoveReplica transitions
                    rule: self.type == 'AddReplica' || self.type == 'RemoveReplica'
                      || self.type == 'ForceRemoveReplica' || !has(self.replicaType)
                  - message: replicaType is required for AddReplica, RemoveReplica,
                      ForceRemoveReplica transitions
                    rule: '!(self.type == ''AddReplica'' || self.type == ''RemoveReplica''
                      || self.type == ''ForceRemoveReplica'') || has(self.replicaType)'
                  - message: fromReplicaType and toReplicaType must only be set for
                      ChangeReplicaType transitions
                    rule: self.type == 'ChangeReplicaType' || (!has(self.fromReplicaType)
                      && !has(self.toReplicaType))
                  - message: fromReplicaType and toReplicaType are required for ChangeReplicaType
                      transitions
                    rule: self.type != 'ChangeReplicaType' || (has(self.fromReplicaType)
                      && has(self.toReplicaType))
                  - message: fromSystemNetworkNames must only be set for ChangeSystemNetworks
                      transitions
                    rule: self.type == 'ChangeSystemNetworks' || !has(self.fromSystemNetworkNames)
                  - message: toSystemNetworkNames must only be set for ChangeSystemNetworks
                      transitions
                    rule: self.type == 'ChangeSystemNetworks' || !has(self.toSystemNetworkNames)
                  - message: fromSystemNetworkNames is required for ChangeSystemNetworks
                      transitions
                    rule: self.type != 'ChangeSystemNetworks' || has(self.fromSystemNetworkNames)
                  - message: toSystemNetworkNames is required for ChangeSystemNetworks
                      transitions
                    rule: self.type != 'ChangeSystemNetworks' || has(self.toSystemNetworkNames)
                type: array
                x-kubernetes-list-type: atomic
            required:
            - datamesh
            type: object
        required:
        - metadata
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.replicatedVolumeName
    served: true
    storage: true
    subresources:
      status: {}

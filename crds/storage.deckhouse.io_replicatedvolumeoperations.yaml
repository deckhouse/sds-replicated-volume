---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    module: sds-replicated-volume
  name: replicatedvolumeoperations.storage.deckhouse.io
spec:
  group: storage.deckhouse.io
  names:
    kind: ReplicatedVolumeOperation
    listKind: ReplicatedVolumeOperationList
    plural: replicatedvolumeoperations
    shortNames:
    - rvo
    singular: replicatedvolumeoperation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicatedVolumeName
      name: Volume
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.conditions[?(@.type=='Completed')].status
      name: Completed
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ReplicatedVolumeOperation represents an operation to update volume configuration
          or resolve eligible nodes conflicts for a ReplicatedVolume.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ReplicatedVolumeOperationSpec defines the desired state of a ReplicatedVolumeOperation.
              All fields are immutable after creation.
            properties:
              replicatedVolumeName:
                description: ReplicatedVolumeName is the name of the ReplicatedVolume
                  this operation targets.
                maxLength: 120
                minLength: 1
                type: string
              type:
                description: Type specifies the operation type.
                enum:
                - UpdateConfiguration
                - ResolveEligibleNodesConflict
                type: string
              updateConfigOptions:
                description: |-
                  UpdateConfigOptions provides additional options for UpdateConfiguration operations.
                  Only allowed when Type is UpdateConfiguration (validated by CEL).
                properties:
                  resolveEligibleNodes:
                    default: false
                    description: |-
                      ResolveEligibleNodes indicates whether this operation should also resolve
                      eligible nodes conflicts when updating configuration.
                      Determined by EligibleNodesConflictResolutionStrategy in the RSC spec.
                    type: boolean
                type: object
            required:
            - replicatedVolumeName
            - type
            type: object
            x-kubernetes-validations:
            - message: spec is immutable
              rule: self == oldSelf
            - message: updateConfigOptions can only be set when type is UpdateConfiguration
              rule: self.type == 'UpdateConfiguration' || !has(self.updateConfigOptions)
          status:
            description: ReplicatedVolumeOperationStatus represents the current state
              of a ReplicatedVolumeOperation.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

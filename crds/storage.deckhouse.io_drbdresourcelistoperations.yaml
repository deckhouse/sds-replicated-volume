---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    module: sds-replicated-volume
  name: drbdresourcelistoperations.storage.deckhouse.io
spec:
  group: storage.deckhouse.io
  names:
    kind: DRBDResourceListOperation
    listKind: DRBDResourceListOperationList
    plural: drbdresourcelistoperations
    shortNames:
    - drbdrlop
    singular: drbdresourcelistoperation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              drbdResourceNames:
                items:
                  maxLength: 253
                  minLength: 1
                  pattern: ^[0-9A-Za-z.+_-]*$
                  type: string
                maxItems: 32
                minItems: 2
                type: array
                x-kubernetes-list-type: set
                x-kubernetes-validations:
                - message: drbdResourceNames is immutable
                  rule: self == oldSelf
              type:
                description: DRBDResourceListOperationType represents the type of
                  operation to perform on a list of DRBD resources.
                enum:
                - ConfigurePeers
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
            required:
            - drbdResourceNames
            - type
            type: object
          status:
            properties:
              results:
                description: Results holds per-resource results. Each agent SSA-applies
                  its entry keyed by drbdResourceName.
                items:
                  properties:
                    completedAt:
                      format: date-time
                      type: string
                    drbdResourceName:
                      minLength: 1
                      type: string
                    message:
                      maxLength: 1024
                      type: string
                    phase:
                      description: DRBDOperationPhase represents the phase of a DRBD
                        operation.
                      type: string
                    startedAt:
                      format: date-time
                      type: string
                  required:
                  - drbdResourceName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - drbdResourceName
                x-kubernetes-list-type: map
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

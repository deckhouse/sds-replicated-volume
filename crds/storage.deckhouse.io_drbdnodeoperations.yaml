---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    module: sds-replicated-volume
  name: drbdnodeoperations.storage.deckhouse.io
spec:
  group: storage.deckhouse.io
  names:
    kind: DRBDNodeOperation
    listKind: DRBDNodeOperationList
    plural: drbdnodeoperations
    shortNames:
    - drbdnop
    singular: drbdnodeoperation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |
          Interface for managing node-level DRBD operations in the cluster.

          The resource is created to perform a maintenance operation on a specific node; the agent on that node performs the operation and reports the result in the status.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.

              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
            x-doc-skip: true
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.

              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
            x-doc-skip: true
          metadata:
            type: object
          spec:
            properties:
              nodeName:
                description: |
                  Name of the node where the operation will be performed.

                  > **Caution.** This field is immutable and cannot be changed after the resource is created.
                maxLength: 253
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: nodeName is immutable
                  rule: self == oldSelf
              type:
                description: |-
                  Type of the DRBD node operation.

                  Might be:
                  - `UpdateDRBD`: Update DRBD on the node.

                  > **Caution.** This field is immutable and cannot be changed after the resource is created.
                enum:
                - UpdateDRBD
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
            required:
            - nodeName
            - type
            type: object
          status:
            properties:
              completedAt:
                description: |
                  Time when the operation completed.
                format: date-time
                type: string
              message:
                description: |
                  Additional human-readable message about the current operation state.
                maxLength: 1024
                type: string
              phase:
                description: |-
                  Current phase of the operation.
                type: string
              startedAt:
                description: |
                  Time when the operation started.
                format: date-time
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.nodeName
    - jsonPath: .status.phase
    served: true
    storage: true
    subresources:
      status: {}

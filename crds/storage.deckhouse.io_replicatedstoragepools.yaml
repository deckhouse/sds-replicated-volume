---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    backup.deckhouse.io/cluster-config: "true"
    heritage: deckhouse
    module: sds-replicated-volume
  name: replicatedstoragepools.storage.deckhouse.io
spec:
  group: storage.deckhouse.io
  names:
    kind: ReplicatedStoragePool
    listKind: ReplicatedStoragePoolList
    plural: replicatedstoragepools
    shortNames:
    - rsp
    singular: replicatedstoragepool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.reason
      name: Reason
      priority: 1
      type: string
    - description: The age of this resource
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReplicatedStoragePool is a Kubernetes Custom Resource that defines
          a configuration for Linstor Storage-pools.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Defines desired rules for Linstor's Storage-pools.
            properties:
              lvmVolumeGroups:
                description: |-
                  An array of names of LVMVolumeGroup resources, whose Volume Groups/Thin-pools will be used to allocate
                  the required space.

                  > Note that every LVMVolumeGroup resource has to have the same type Thin/Thick
                  as it is in current resource's 'Spec.Type' field.
                items:
                  properties:
                    name:
                      description: Selected LVMVolumeGroup resource's name.
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9-.]{0,251}[a-z0-9])?$
                      type: string
                    thinPoolName:
                      description: Selected Thin-pool name.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              type:
                description: |-
                  Defines the volumes type. Might be:
                  - LVM (for Thick)
                  - LVMThin (for Thin)
                enum:
                - LVM
                - LVMThin
                type: string
                x-kubernetes-validations:
                - message: Value is immutable.
                  rule: self == oldSelf
            required:
            - lvmVolumeGroups
            - type
            type: object
          status:
            description: Displays current information about the state of the LINSTOR
              storage pool.
            properties:
              phase:
                description: |-
                  The actual ReplicatedStoragePool resource's state. Might be:
                  - Completed (if the controller received correct resource configuration and Linstor Storage-pools configuration is up-to-date)
                  - Updating (if the controller received correct resource configuration and Linstor Storage-pools configuration needs to be updated)
                  - Failed (if the controller received incorrect resource configuration or an error occurs during the operation)
                enum:
                - Updating
                - Failed
                - Completed
                type: string
              reason:
                description: The additional information about the resource's current
                  state.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
